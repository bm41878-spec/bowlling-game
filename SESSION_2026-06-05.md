# 세션 로그 — 2026-06-05

> **테마**: 코드 최적화 + 게임 상태 흐름 정리 + 해상도 대응 UI.
> **선행 컨텍스트**: `README.md` §14, `AI_PROMPT_REFERENCE.md`, `NEXT_SESSION.md` (ResultUI 작업은 이번 세션에서 보류).

---

## 0. 한눈 요약

| # | 작업 | 상태 | 영향 파일 |
|---|---|---|---|
| 1 | F1+O1 — 공 리셋 단일 경로 + BallSpawnPoint 캐싱 | ✅ 검증 완료 | `BowlingBall.cs`, `AI_PROMPT_REFERENCE.md` |
| 2 | 스트라이크/스페어 후 공 위치 미초기화 버그 수정 | ✅ 검증 완료 | `BowlingBall.cs` |
| 3 | 점수판 Canvas 해상도 일관성 (CanvasScaler) | ✅ 검증 완료 | `Assets/Scenes/Game.unity` |

---

## 1. F1+O1 — 공 리셋 단일 경로 + BallSpawnPoint 캐싱

### 동기
- **F1**: 한 투구 종료 시 (Scoring → AimingPosition) `ball.ResetToStartPosition()` 이 **2회** 호출되고 있었음.
  - 1회차: `ThrowTransitionController.HandlePostThrow()` 의 분기 끝.
  - 2회차: 상태 캐스케이드 중 `BowlingBall.HandleStateChanged(AimingPosition)` 의 명시적 리셋.
- **O1**: 그 두 위치에서 매번 `GameObject.Find("BallSpawnPoint")` 호출 (씬 풀 스캔 비용).

### 변경
- `Assets/Scripts/Gameplay/BowlingBall.cs`:
  - `Awake()` 에서 `BallSpawnPoint` Transform **1회 캐싱**.
  - Fallback 상수 `FallbackSpawnPosition = (0, 0.15, 0.5)` 정적 readonly.
  - `HandleStateChanged()` 의 `next == AimingPosition` 분기 **제거**.
  - `ResetToStartPosition()` 이 캐시 사용.
- `AI_PROMPT_REFERENCE.md` §4-1 시퀀스 다이어그램 동기화 — BowlingBall 의 AimingPosition 리셋 줄 제거 + 단일 리셋 경로 주석.

### 효과
- 게임당 수십 회의 `GameObject.Find` 풀-스캔 제거.
- 공 위치 리셋이 `GameManager.BeginGame` + `ThrowTransitionController.HandlePostThrow` 두 경로로만 일원화 — 흐름 추적 단순화.

### 부작용
- **회귀 발생 → 다음 섹션에서 처리.**

---

## 2. 스트라이크/스페어 후 공 위치 미초기화 버그

### 증상
스트라이크 또는 스페어로 프레임이 종료되면 다음 프레임에서 공이 spawn 위치에 보이지 않음. 카메라, BallAimer 의 PingPong, PowerGauge UI 는 모두 정상.

### 진단 (3단계 추론)

**1차 단서** — Game.unity 의 BowlingBall Rigidbody 직렬화 데이터:
```
m_Interpolate: 1          ← Interpolation 활성
m_CollisionDetection: 2   ← ContinuousDynamic CCD
```

**핵심 원리**: Unity 6 기본값 `Physics.autoSyncTransforms = false` 환경에서:
- `transform.position` 만 바꾸면 `Rigidbody.position` 으로 자동 전파되지 않음.
- F1 이전엔 `BowlingBall.HandleStateChanged` 의 2차 `ResetBall` 호출 중 `rb.isKinematic = true` 토글이 **암묵적 sync 지점** 역할을 우연히 해 줌.
- F1 변경 이후 그 보험이 사라져 `Rigidbody.position` 이 settle 위치로 stale 한 채 dynamic 으로 복귀.

### 1차 수정 (실패)
`ResetBall()` 에 `rb.position = position` + `rb.rotation = identity` 추가.

**결과**: 위치 미초기화 해결되었으나 **y 좌표 누적 드리프트** 발생 — 매 리셋마다 공이 미세하게 아래로 이동, 결국 지면 아래로 빠져 보이지 않음.

### 사용자 진단 → 옵션 선택
사용자 직접 진단: 운동 물리 값 (velocity, angularVelocity, 누적 force, contact buffer, Interpolation `previousPosition`) 이 초기화되지 않은 채 위치만 바뀌어서 잔여 상태 누적.

**두 옵션 비교**:
| 기준 | A: 물리값 초기화 | B: 파괴/재스폰 |
|---|---|---|
| 구현 비용 | 낮음 (한 함수 수정) | 중간 (팩토리 + 재배선) |
| 런타임 비용 | 거의 0 | GameObject 인스턴스화 hitch |
| Unity 내부 상태 의존 | 있음 (Sleep/Sync 명시 필요) | 없음 |
| 인스펙터 안정성 | 높음 | 낮음 (`GameManager.ball`, `transitionController.ball`, `CameraFollow.ballTransform` 재캐싱 필요) |
| GC 부담 | 0 | 매 프레임 전환마다 | 

**사용자 선택**: **A** (인스펙터 배선 안정성 + 1개 인스턴스 구조라 옵션 B 의 이점 미미).

### 최종 수정 (옵션 A — 강화 ResetBall 패턴)

`BowlingBall.ResetBall()` 6단계:
1. `velocity` / `angularVelocity` = 0 (**동적 일 때만** — kinematic warning 회피).
2. `rb.isKinematic = true` (CCD / Interpolation / 중력 영향 차단).
3. `transform.position` / `rotation` 갱신.
4. **`Physics.SyncTransforms()`** — Transform → 물리 엔진 강제 sync.
5. **`rb.Sleep()`** — force / contact buffer / `previousPosition` 누적 상태 청소.
6. `rb.isKinematic = false` 동적 복귀.

검증: 5프레임 연속 스트라이크에서도 y 드리프트 0.

---

## 3. 점수판 Canvas 해상도 일관성

### 증상
랩탑 (2880×1800, 16:10) 과 데스크탑 (QHD 2560×1440, 16:9) 에서 점수판 텍스트의 위치·크기가 미세하게 다르게 표시.

### 진단
Game.unity 두 Canvas 의 CanvasScaler 설정 비교:

```
HUD_Canvas:
  m_UiScaleMode: 1                       ← ScaleWithScreenSize ✓
  m_ReferenceResolution: 1920×1080
  m_MatchWidthOrHeight: 0.5

Canvas (점수판):
  m_UiScaleMode: 0                       ← ConstantPixelSize ❌ 원인
  m_ReferenceResolution: 800×600
  m_MatchWidthOrHeight: 0
```

`ConstantPixelSize` 는 UI 요소를 **픽셀 절대값으로 고정** — 100px 요소가 2880×1800 에선 3.47% 폭, 2560×1440 에선 3.91% 폭. 결과적으로 해상도에 따라 상대 크기가 변동.

### 수정
`Assets/Scenes/Game.unity` 의 Canvas (fileID 1516199509) CanvasScaler:
- `m_UiScaleMode`: 0 → **1** (ScaleWithScreenSize).
- `m_ReferenceResolution`: 800×600 → **1920×1080** (HUD_Canvas 와 통일).
- `m_MatchWidthOrHeight`: 0 → **0.5** (Width/Height 균형 스케일).

### 잠재 후속 작업
비율 차이 (16:10 vs 16:9) 에서 모서리 마진이 추가로 어긋나면 5개 텍스트 (`total_score_n`, `frame_n`, `frame_N_first`, `frame_/`, `frame_N_sec`) 의 RectTransform Anchor 를 모서리 기준으로 재설정 필요. 현재는 보류 (검증상 충분).

---

## 변경된 파일

| 파일 | 변경 요약 |
|---|---|
| `Assets/Scripts/Gameplay/BowlingBall.cs` | spawnPoint 캐싱, HandleStateChanged 정리, ResetBall 6단계 강화 |
| `Assets/Scenes/Game.unity` | 점수판 Canvas CanvasScaler 모드/해상도/Match 변경 |
| `AI_PROMPT_REFERENCE.md` | §4-1 시퀀스에서 BowlingBall AimingPosition 리셋 제거 |

---

## 검증 완료 체크리스트

- [x] Play 모드 진입 시 1프레임 정상 시작 + 콘솔 에러·경고 0.
- [x] 스트라이크 → 다음 프레임 공이 spawn 위치 정상 (y 드리프트 없음).
- [x] 스페어 → 다음 프레임 공 위치 정상.
- [x] 연속 5프레임 완주 누적 드리프트 없음.
- [x] 랩탑 2880×1800 / 데스크탑 QHD 양쪽에서 점수판 UI 일관성 확인.
- [x] 컴파일 에러·경고 0건.

---

## 다음 작업 후보

본 세션에서 **식별만 하고 미실행한 항목**:

### 게임 상태 흐름 (F1 완료, F2~F4 미실행)
- **F2**. `GameState.Ready` 가 사실상 dead path — `GameStateManager.EnterState(Ready)` 의 `OnReadyConfirm` 결선 제거 또는 의도된 "스페이스로 시작" UX 살림.
- **F3**. `GameStateManager.ChangeState` 의 발행 순서 명시화 — 현재 `ExitState → CurrentState 갱신 → OnStateChanged → EnterState`. 향후 `EnterState` 에 초기화 로직 들어가면 구독자가 미초기화 상태를 관찰. 컨벤션 문서화 필요.
- **F4**. `RestartGame` 의 중간 상태 인터럽트 가드 — Scoring 도중 R 키 입력 시 `OnTransitionComplete` 이중 발행 가능성 차단.

### 코드 최적화 (O1 완료, O2~O5 미실행)
- **O2**. `PinManager.GetStandingPins / GetFallenPins` 매 호출 `new List<Pin>` → 재사용 버퍼 또는 카운트만 반환.
- **O3**. `Pin.Update()` 매 프레임 `Vector3.Angle` (10핀 × 60fps) → `_rb.IsSleeping()` 가드 또는 `OnCollisionEnter` 이벤트 드리븐.
- **O4**. `PowerGaugeUI.Update()` 매 프레임 string 보간 / `new Vector2` → 값 변경 시에만 갱신.
- **O5**. `PhysicsSettleDetector.DetectLoop()` 의 `yield return null` → `WaitForFixedUpdate` (물리 폴링은 물리 틱과 맞추는 게 정확성·비용 모두 이득).

### 기획된 다른 작업
- **ResultUI** — 결과 화면 5단계 점진 구현 (NEXT_SESSION.md 의 원래 계획, 사용자 확정 대기 중).
- **메인메뉴 → 게임 씬 전이** — `mainmenu.unity` 비어있음.
- **JSON SaveSystem** (Phase 8) — `GameRecord` / `SaveData` 스켈레톤만 존재.
- **10프레임 동적 점수판** (README §6) — 현재는 단일 프레임만 표시.

---

*세션 종료: 2026-06-05. 다음 세션 재개 시 본 문서 + `NEXT_SESSION.md` 참고.*
